import type { MapOptions } from '@automapper/types';
import type { NestInterceptor } from '@nestjs/common';
export declare const MapInterceptor: (to: unknown, from: unknown, options?: {
    isArray?: boolean;
    mapperName?: string;
} & MapOptions) => NestInterceptor;
