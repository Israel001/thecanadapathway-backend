import type { MapOptions } from '@automapper/types';
import type { PipeTransform } from '@nestjs/common';
export declare const MapPipe: (to: unknown, from: unknown, options?: {
    isArray?: boolean;
    mapperName?: string;
} & MapOptions) => PipeTransform;
