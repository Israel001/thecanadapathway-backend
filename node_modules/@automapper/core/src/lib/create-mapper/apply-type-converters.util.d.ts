import type { Converter, Mapping, PrimitiveConstructorWithDate, ValueSelector } from '@automapper/types';
/**
 *
 * Apply converters if available to mapInitialize on createMap
 *
 * @param mapping
 * @param typeConverters
 */
export declare function applyTypeConverters(mapping: Mapping, typeConverters: WeakMap<PrimitiveConstructorWithDate, WeakMap<PrimitiveConstructorWithDate, ValueSelector | Converter>>): void;
